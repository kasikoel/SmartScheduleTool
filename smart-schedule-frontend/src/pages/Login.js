import React from "react";

const LoginPage = () => {
    return (
        <div>
            <h1>Login functionality removed.</h1>
            <p>No authentication is required to use this app.</p>
        </div>
    );
};

export default LoginPage;